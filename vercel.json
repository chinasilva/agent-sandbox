{
  "version": 2,
  "buildCommand": "npm install --no-optional",
  "installCommand": "npm install --no-optional",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "/api/server.js"
    },
    {
      "source": "/health",
      "destination": "/api/server.js"
    }
  ]
}
